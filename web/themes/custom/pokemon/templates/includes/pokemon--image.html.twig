{#
/**
 * @file
 *
 * Available variables:
 * - label: The label of the pokemon
 * - pokeapi_id: The id of the pokemon into the PokeApi
 */
#}

{% set img_source = 'https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/' %}

{% set img_url = img_source ~ pokeapi_id ~ '.png' %}

<img class="pokemon--image" src="{{ img_url }}" alt="{{ label|render|striptags|trim }}">