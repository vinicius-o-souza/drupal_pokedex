/**
 * @file
 * Pokemon behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.pokemon = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
